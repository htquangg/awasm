server:
  http:
    addr: 0.0.0.0:3000

db:
  host: host.docker.internal
  port: 3306
  user: "root"
  password: "toor"
  schema: "dev-local-awasm-001"
  charset: "utf8mb4"
  log_sql: true
  sll_mode: false
  max_idle_conns: 1000
  max_open_conns: 100
  conn_max_lifetime: 300

i18n:
  bundle_dir: "/a-wasm/i18n"
